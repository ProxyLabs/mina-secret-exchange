// Tests the contract

import {
  init,
  submitSolution,
  getState,
  swapToken,
  deployDebugContract,
} from "../dist/snapp/snapp";

describe("Tests contract deployment and usage", () => {
  test("adds 1 + 2 to equal 3", async () => {
    // let res = await deployDebugContract();
    // expect(res).toBe(true);
    // let res2 = await submitSolution(0);
    // console.log(res2);
  });
});
